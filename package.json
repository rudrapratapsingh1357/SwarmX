{
  "name": "swarmx",
  "version": "1.0.0",
  "description": "Fullstack deployment configuration for SwarmX",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build": "npm run build:frontend",
    "start": "cd backend && pip install -r requirements.txt && python main.py"
  }
}
